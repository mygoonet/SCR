# Фикс разметки SCRP — 2026-08-27

## Что сломалось

`scrpcheck --sign 0000242398`: проверка разметки таблицы и пути до модалки
сертификата — OK, но полный прогон упал на шаге 5 (модалка «Выбор
доверенности»):

```
FULL SIGN: FAIL — poa modal: timeout 30s waiting for:
document.querySelector('[data-tid="representative-poa-list-item"]') !== null
```

Диагностика (cmd/poadump, дамп DOM после клика «Выбрать» в модалке
сертификата) показала: модалка «Выбор доверенности» открывается, но пункт
списка доверенности получил новый `data-tid`:

- старый: `representative-poa-list-item` — **пропал** (0 элементов);
- новый: `scoped-poa-list-item` — присутствует (1 элемент), внутри
  `scoped-poa-list`, рядом `scoped-poa-number`, `scoped-poa-inn`,
  `scoped-poa-kpp`, `scoped-poa-list-item-status`,
  `scoped-poa-radio-button`.

Кнопка в футере модалки: `data-tid="continue-with-poa-button"` — **не
поменялась**, но текст кнопки теперь «Сохранить» (был «Продолжить»). Код по
тексту не завязан, менять ничего не нужно.

## Новые селекторы (проверено на сайте 2026-08-27)

| Файл | Старый селектор | Новый селектор |
|------|-----------------|----------------|
| SCRP/sign.go:244 | `[data-tid="representative-poa-list-item"]` | `[data-tid="scoped-poa-list-item"]` |
| SCRP/sign.go:247 (экранированный вариант) | `[data-tid=\"representative-poa-list-item\"]` | `[data-tid=\"scoped-poa-list-item\"]` |

## Проверка

`cmd/poaverify` (дошёл до модалки доверенности на тестовой накладной
000000420, кликнул новый селектор `scoped-poa-list-item`):
кнопка `continue-with-poa-button` стала enabled — новый селектор работает.
ESC, ничего не подписано.

## Статус
применено 2026-08-27 (apply-fix.py, бэкап .backup-20260827-175539), чекер OK:
`scrpcheck --sign 000000420` → RESULT: OK + FULL SIGN: OK.

# apply-fix.py: пары старый<TAB>новый
[data-tid="representative-poa-list-item"]	[data-tid="scoped-poa-list-item"]
[data-tid=\"representative-poa-list-item\"]	[data-tid=\"scoped-poa-list-item\"]
